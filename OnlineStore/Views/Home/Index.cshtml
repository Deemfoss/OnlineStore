@model IEnumerable<OnlineStore.Domain.Entities.Store>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Adress)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.WorkTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
   using (Ajax.BeginForm("Edit", new AjaxOptions { UpdateTargetId = "results" }))
   {
        
     <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Adress)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WorkTime)
        </td>

        <td>
           
                @Ajax.ActionLink("Productts", "Edit",new {id=item.Id }, new AjaxOptions { UpdateTargetId = "results" })
            
                
          
        </td>
    </tr>
}
}
</table>
<div id="results"></div>
@Scripts.Render("~/scripts/jquery-1.7.1.js")
@Scripts.Render("~/scripts/jquery.unobtrusive-ajax.js")